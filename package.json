{
  "name": "harbor-webapp",
  "version": "1.0.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Harbor — calm life management. PWA + Capacitor (App Store / Play).",
  "scripts": {
    "start": "powershell -ExecutionPolicy Bypass -File scripts/start-server.ps1",
    "preview": "powershell -ExecutionPolicy Bypass -File scripts/start-server.ps1",
    "zip": "powershell -ExecutionPolicy Bypass -File scripts/create-deploy-zip.ps1",
    "cap:prepare": "powershell -ExecutionPolicy Bypass -File scripts/cap-prepare.ps1",
    "cap:sync": "npm run cap:prepare && npx cap sync",
    "cap:android": "npm run cap:sync && npx cap open android",
    "cap:ios": "npm run cap:sync && npx cap open ios",
    "cap:ios:sync": "npm run cap:prepare && npx cap sync ios",
    "store:prepare": "npm run cap:prepare && npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.3",
    "@capacitor/core": "^6.2.0",
    "@capacitor/geolocation": "^6.1.1",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/local-notifications": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.4",
    "@capacitor/status-bar": "^6.0.3",
    "@ebarooni/capacitor-calendar": "^6.7.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
